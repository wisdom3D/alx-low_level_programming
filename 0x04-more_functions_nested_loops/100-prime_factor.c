#include <stdio.h>

/**
 * main - calculate largest prime of 612852475143
 *
 * Return: Success Always
 */

int mai(void)
{
		long int x = 612852475143.
		long int pf;

		for (pf = 2, pf < x; pf++)
		{
			of (x % pf == 0)
			{
				x = x / pf;
			}
		}
		print("%ld\n", pf);
		return (0);
		
}
