#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	int  numbr;

	for (numbr = 0 ; numbr < 10 ; numbr++)
		printf("%d", numbr);
	putchar('\n');
	return (0);
}
